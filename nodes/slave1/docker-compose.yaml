version: "3.9"

services:
  mariadb-slave1:
    image: mariadb:10
    deploy:
      replicas: 1
      update_config:
        order: start-first
        delay: 1s
      restart_policy:
        condition: on-failure
        delay: 5s
        window: 120s
        max_attempts: 2
      # resources:
      #   limits:
      #     cpus: "0.5"
      #     memory: "512M"
    environment:
      TZ: Asia/Jakarta
      MYSQL_ROOT_PASSWORD: SLAVE1_ROOT_PASSWORD_SET
    volumes:
      - CHANGE_DIR:/var/lib/mysql
      - ./conf/mysql.cnf:/etc/mysql/conf.d/mysql.cnf:ro
      - ./initdb:/docker-entrypoint-initdb.d
    ports:
      - 3302:3302
    networks:
      - mariadb-network

networks:
  mariadb-network:
    external: true
